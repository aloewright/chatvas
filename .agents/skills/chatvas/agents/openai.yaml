interface:
  display_name: "Chatvas"
  short_description: "Repo-specific patterns and workflows for chatvas"
  default_prompt: "Use the chatvas repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true